node_modules
dist
dist-preview
.env
.env.*
.DS_Store
# .claude/launch.json se versiona (config del preview de Claude Code)
# Permisos locales de cada PC y copias aisladas del repo que crean los workflows de Claude Code
.claude/settings.local.json
.claude/worktrees/
design/
tools/salida/
